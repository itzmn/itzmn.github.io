<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[java集合总纲]]></title>
    <url>%2F2018%2F12%2F05%2Fjava%E9%9B%86%E5%90%88%E6%80%BB%E7%BA%B2%2F</url>
    <content type="text"><![CDATA[java集合集合是用来存储数据的，以前有了数组为什么还要集合呢，因为数组必须要声明长度，集合不需要，更加灵活， 集合是在java.util包中的，也有一部分在java.util.concurrent 这里面是一些支持并发编程的集合类 概况java的集合分为两种，一种是实现了map接口的key-value形式的，每个位置存放一对数据。一种是实现了Collection接口的每个位置存放一个数据， 具体实现CollectionCollection是继承了Iterable接口的 我们看一下Iterable接口是干嘛的，接口的简介说实现此接口，可以让其成为可以使用for-each的目标，也就是说实现这个接口的集合，都可以被遍历。 12// 这是接口内部的方法，返回类型元素的迭代器Iterator&lt;T&gt; iterator(); Collection接口下有三个子接口 List ArrayList LinkedList Set HashSet LinkedHashSet TreeSet Queue Deque LinkedList 现在对每一种进行简单介绍 List 是一个有序，可重复的集合。添加数据，获取数据也是按照添加数据的顺序 ArrayList ArrayList是基于数组实现的List类，它封装了一个动态的增长的、允许再分配的Object[]数组。 LinkedList 是基于链表实现的，他还实现了Deque接口，可以进行双端队列操作 Set 是一个不可有重复的集合 HashSet 这是一个无序的集合， LinkedHashSet 这是一个有序的集合，底层按照数据添加顺序维护 Queue是一个队列，先入先出 Deque 也是一个可以进行双端队列操作的 LinkedList ArrayQueue Mapmap用于保存一个映射关系的数据的，存储是key-value形式的数据。map不允许存储key相同的数据 HashMap 不能保证数据的顺序性， HashTable SortMap TreeMap 基于红黑树实现，可以根据key对数据进行排序 下面我们会对这些集合类，进行进一步探讨]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java集合</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[操作mysql]]></title>
    <url>%2F2018%2F12%2F05%2F%E6%93%8D%E4%BD%9Cmysql%2F</url>
    <content type="text"><![CDATA[mysql基础架构先上图 首先第一层，当请求过来的连接处理，授权认证，安全都是在这一层 第二层是mysql的核心服务功能，包括查询解析，分析，优化，缓存以及所有的内置函数(日期，时间，数学和加密函数),所有跨存储引擎的功能都在这一层实现，如存储过程，触发器，视图 第三次则是存储引擎。负责对mysql中的数据进行存储和提取。 基础操作操作数据库创建删除 123create database test // 创建drop database test // 删除show databases // 查看所有的数据库 增删改查创建操作表 1234567891011create table 表名( 列名 类型(长度) 限制条件(not null,uniqe ), primary key (主键列))存储引擎 字符集例：CREATE TABLE `user` ( `id` int(11) NOT NULL AUTO_INCREMENT, `name` varchar(255) DEFAULT NULL, `age` int(11) DEFAULT NULL, PRIMARY KEY (`id`)) ENGINE=InnoDB AUTO_INCREMENT=3 DEFAULT CHARSET=utf8mb4 删除表 1drop table 表名 查询查询使用join连接字，分为左连接(left join)，右连接(right join)，内连接(inner join)，全连接(full join) 在对多个表进行连接查询的时候，其实是在对多个表中符合条件的数据进行笛卡尔积 左连接 以连接表的左表为标准，左表中的数据全部显示，而右边中的数据只显示满足条件的 右连接 以连接表的右表为标准，右表中的数据全部显示，而左边的只显示满足条件的，没有则显示null 内连接 显示两表中数据满足条件的数据 全连接 显示两表所有数据，mysql暂不支持 删除delete from 表名 where 条件 更新update 表名 set 列=‘’ where 条件 插入insert into 表名（字段） values（字段值） 存储引擎mysql有多种存储引擎，但是常见的就是两种 Myisam和Innodb，下面看看他们有什么区别 Myisam Innodb 索引的实现原理 B+Tree （非聚集索引） B+Tree（聚集索引） 锁 支持表锁 支持表锁，行锁（默认行锁，当没有操作索引列，上升为表锁） 数据文件 .myi(索引文件) .frm(表的结构文件) .myd(表的数据文件) .frm (表的结构文件) .ibd(存储表数据文件) 事务 不支持 支持事务 适用情况 多查询，少修改 多查询，多修改 索引索引是对数据进行定位，在对数据进行操作使用，如果使用索引，那么效率则会大大加快，在不同的存储引擎下索引的实现不同，数据库的存储引擎已经说了 基础操作创建ALTER TABLE用来创建普通索引、UNIQUE索引或PRIMARY KEY索引 123ALTER TABLE table_name ADD INDEX index_name (column_list) ALTER TABLE table_name ADD UNIQUE (column_list)ALTER TABLE table_name ADD PRIMARY KEY (column_list) CREATE INDEX可对表增加普通索引或UNIQUE索引 12CREATE INDEX index_name ON table_name (column_list)CREATE UNIQUE INDEX index_name ON table_name (column_list) 删除123DROP INDEX index_name ON talbe_nameALTER TABLE table_name DROP INDEX index_nameALTER TABLE table_name DROP PRIMARY KEY // 仅在删除主键索引时使用，因为主键只有一个，不需要指定索引名称 原理数据库的索引都是通过B+树实现，但是不同存储引擎的实现方法也不同，先说一下B+树，B+树是在B树的基础上实现的，B树是每个节点都保存着数据，但是B+树只在叶子节点保存数据，其余的节点都是没有数据的 Myisam 我们已经知道，Myisam在存储数据的时候，有三个文件，为什么呢，因为其中一个文件就是存储索引的，实现索引的B+树的叶子节点存储的是索引对应数据的地址。我们在对数据操作的时候，如查询，那么他会先在索引文件查询，查到该数据的地址，然后根据地址去数据存储文件查，得到数据，是非聚集索引 Innodb Innodb是聚集索引，首先他会根据id建立索引，然后将数据存储在索引树上，如果还根据其他列建立索引，那么索引树存储的值是他的id，然后根据id去id索引树查找到真正的数据 如何创建索引 最左原则，在创建联合索引的时候，多列数据组成索引，在B+树中也是按照多列进行存储，那么如果在查询的时候，最左边的条件不是索引，那么就无法命中索引，那么索引就无效了 尽量选择查询频率较高的字段， 尽量扩展索引，而不是增加索引，如对a列已经做了索引，现在想对b列也做索引，那么可以修改以前的索引为(a,b)， 小技巧 explain sql 可以查看sql的执行计划，对sql进行优化]]></content>
      <categories>
        <category>java</category>
        <category>mysql</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[java的比较器小结]]></title>
    <url>%2F2018%2F11%2F16%2Fjava%E7%9A%84%E6%AF%94%E8%BE%83%E5%99%A8%E5%B0%8F%E7%BB%93%2F</url>
    <content type="text"><![CDATA[摘要java的比较器分两种，也即是内外比较器，内部比较器是comparable接口，外部比较器是comparator接口，为什么分内外比较器呢。 内部比较器：comparable，当需要对某个类的对象进行排序的时候，则需要该类实现comparable接口，重写comparaTo方法，实现这个接口的类的对象列表，可使用Array.sort或者Collections.sort进行排序， 外部比较器：comparator接口，匿名内部类的形式存在，重写内部的compare方法，可对对象数组或者集合使用Arrays.sort(数组，比较器)或者Collections.sort(集合，比较器)进行排序 compara和comparaTo方法共同之处，就是如果比较的数比被比较的数小返回-1， 例： a.comparaTo(b) a小，返回-1 ，a大返回1，compara同理 ComparablecomparaTo方法类实现comparable接口，都要重写comparaTo方法，举个栗子， Integer类中的方法 1234567891011//Integer实现了Comparable接口public final class Integer extends Number implements Comparable&lt;Integer&gt; &#123;//比较两个数值public int compareTo(Integer anotherInteger) &#123; return compare(this.value, anotherInteger.value);&#125;//比较两个数值，如果前面一个大，返回1，小返回-1，相等返回0public static int compare(int x, int y) &#123; return (x &lt; y) ? -1 : ((x == y) ? 0 : 1);&#125; String类中 123456789101112131415161718192021222324252627282930//实现了接口public final class String implements java.io.Serializable, Comparable&lt;String&gt;, CharSequence &#123; //String类中的compareTo方法public int compareTo(String anotherString) &#123; //得到调用者的值的长度 int len1 = value.length; //得到比较的值的长度 int len2 = anotherString.value.length; //查看最小长度 int lim = Math.min(len1, len2); char v1[] = value; char v2[] = anotherString.value; int k = 0; //循环到最小长度的值结束 while (k &lt; lim) &#123; char c1 = v1[k]; char c2 = v2[k]; //比较每一个字符 if (c1 != c2) &#123; //返回字符的ASCII码差值 return c1 - c2; &#125; k++; &#125; //如果前面的字符都一样，返回长度差 return len1 - len2; &#125; 以上是java内置好的一些类，实现了接口，重写了方法，如果我们自己类想要实现接口，可以重写这个方法，自定义排序的方法 例： 1234567Class B implements Comparable&lt;B&gt;&#123; int comparaTo(B b)&#123; //return的值按照自己是想怎么排序进行返回 return &#125;&#125; 当我们的类实现了Comparable接口，那么类对象集合，或者数组可使用工具类进行排序。 可以使用 Arrays.sort(数组，null)，Collections.sort(集合，null)进行排序 Comparatorcomparator一般是外比较器，以匿名内部类的形式存在，对数据进行比较 可新建一个比较器的对象，实现比较器内部的compara方法 1234567//新建一个比较器new Comparator&lt;Integer&gt;()&#123; @Override public int compare(Integer str1, Integer str2)&#123; return str1.compareTo(str2); &#125;&#125;; 可使用Arrays.sort(数组，比较器)或者Collections.sort(集合，比较器)进行排序 Collections.sort()了解了比较器，我们看一下一些工具类内到底是如何将数组进行排序的 我们举个栗子，跟踪源码看一下 模拟数据10 14 7 8 12 1234567891011121314ArrayList&lt;Integer&gt; integers = new ArrayList&lt;&gt;(); integers.add(10); integers.add(14); integers.add(7); integers.add(8); integers.add(12); integers.add(3); Collections.sort(integers, new Comparator&lt;Integer&gt;()&#123; @Override public int compare(Integer str1, Integer str2)&#123; return str1.compareTo(str2); &#125; &#125;); 开始调用我们点进源码看一下 1.第一步这是Collections的sort方法，调用了list.sort方法， 1234@SuppressWarnings(&#123;&quot;unchecked&quot;, &quot;rawtypes&quot;&#125;) public static &lt;T&gt; void sort(List&lt;T&gt; list, Comparator&lt;? super T&gt; c) &#123; list.sort(c); &#125; 2.第二步我们点进list.sort()方法，这里他将集合数据转成了数组传入后面。数组默认长度是10，但是只有size个数据。但是list是个接口，所以我们查看其中一个实现类的实现方法，ArrayList的实现方法, 12345678910//这是ArrayList的sort方法。public void sort(Comparator&lt;? super E&gt; c) &#123; final int expectedModCount = modCount; //调用了Arrays.sort 并将比较器传了进来， Arrays.sort((E[]) elementData, 0, size, c); if (modCount != expectedModCount) &#123; throw new ConcurrentModificationException(); &#125; modCount++; &#125; 3.第三步我们点进Arrays.sort方法查看，我们一条一条看 123456789101112public static &lt;T&gt; void sort(T[] a, int fromIndex, int toIndex, Comparator&lt;? super T&gt; c) &#123; if (c == null) &#123; sort(a, fromIndex, toIndex); &#125; else &#123; rangeCheck(a.length, fromIndex, toIndex); if (LegacyMergeSort.userRequested) legacyMergeSort(a, fromIndex, toIndex, c); else TimSort.sort(a, fromIndex, toIndex, c, null, 0, 0); &#125; &#125; 1.如果比较器为null，这个我们一会再看，（记住这个，我们会回来的） 2.rangeCheck()，看名字就可以看出，这是一个检验下标是否越界的方法，就不细说了， 3.下面这个判断，我也不大懂，但是每次他都是进入了else进行排序 4.第四步我们进入TimSort.sort方法，传入的数据是 a:数组数据，也就是集合中的数据，formIndex：0，toIndex：是刚刚集合的size。 123456789101112131415161718 static &lt;T&gt; void sort(T[] a, int lo, int hi, Comparator&lt;? super T&gt; c, T[] work, int workBase, int workLen) &#123; assert c != null &amp;&amp; a != null &amp;&amp; lo &gt;= 0 &amp;&amp; lo &lt;= hi &amp;&amp; hi &lt;= a.length; int nRemaining = hi - lo; if (nRemaining &lt; 2) return; // Arrays of size 0 and 1 are always sorted // If array is small, do a "mini-TimSort" with no merges if (nRemaining &lt; MIN_MERGE) &#123; int initRunLen = countRunAndMakeAscending(a, lo, hi, c); binarySort(a, lo, hi, lo + initRunLen, c); return; &#125;//下面有些代码，都没看了，就看到这 ..... &#125; 我们还是一行一行看 1.断言，判断下标是否正确 2.得到集合的数据长度，并判断是否小于2，也即是判断有没有排序的必要 3.如果大于，判断数据是否小于32，我们看的是小于，大于的代码没看 4.int initRunLen = countRunAndMakeAscending(a, lo, hi, c); ​ 我们看一下这个方法是干什么的 下面点进入，贴代码 4.1 countRunAndMakeAscending代码123456789101112131415161718192021//传入的参数，a是数组，lo是开始位置默认是0，hi是结束位置默认是刚刚集合的长度private static &lt;T&gt; int countRunAndMakeAscending(T[] a, int lo, int hi, Comparator&lt;? super T&gt; c) &#123; assert lo &lt; hi; int runHi = lo + 1; if (runHi == hi) return 1; // Find end of run, and reverse range if descending if (c.compare(a[runHi++], a[lo]) &lt; 0) &#123; // Descending //判断后面一个值是否是小于当前值的。 while (runHi &lt; hi &amp;&amp; c.compare(a[runHi], a[runHi - 1]) &lt; 0) runHi++; reverseRange(a, lo, runHi); &#125; else &#123; // Ascending while (runHi &lt; hi &amp;&amp; c.compare(a[runHi], a[runHi - 1]) &gt;= 0) runHi++; &#125; return runHi - lo; &#125; 解释一下方法的参数 a是数组，lo是开始位置默认是0，hi是结束位置默认是刚刚集合的长度（下面称集合长度），c是比较器 1.断言，开始索引小于传入的集合长度 2.runHi变成了开始坐标加一 3.如果这个数值和集合长度相同，则返回1， 4.判断，并且将runHi加1，在这里我们刚刚在比较器内部的操作效果就显示出来了。在这里，我们将坐标靠后面的数与该坐标数值进行比较，如果这里比较用的就是我们自己写在比较器里面的比较 5.如果返回-1 ，说明后面一个值小，这两个数构成了降序，然后还是接着判断后面一个值和当前的值大小，直到降序被打断，记录这个坐标，一会返回这个坐标。 如果被打断，进入另一个方法，将这一段降序的数组，反转，改成升序， 12345678910// 反转这部分的数组private static void reverseRange(Object[] a, int lo, int hi) &#123; hi--; while (lo &lt; hi) &#123; //交换数组的值 Object t = a[lo]; a[lo++] = a[hi]; a[hi--] = t; &#125; &#125; 6.如果返回大于0，说明后面一个值大，这两个值构成了升序。然后继续比较runHi位置和前面一个位置的数值，如果都是后面的大，就一直向后，直到数值变化为小的，也即是一直是升序，突然升序变了，记录下这个位置并返回这个位置。 4.1结束，回到上一个方法 5,第五步我们返回了一个坐标，现在这个数组的开始到这个坐标以前，已经满足了递增的规律，然后我们需要将整个数据进行排序。进入了 binarySort(a, lo, hi, lo + initRunLen, c);方法 12345678910111213141516171819202122232425262728293031323334// 传入数组，开始坐标，结束坐标，start是上一步，0加上已经排序好的坐标的后一位private static &lt;T&gt; void binarySort(T[] a, int lo, int hi, int start, Comparator&lt;? super T&gt; c) &#123; assert lo &lt;= start &amp;&amp; start &lt;= hi; if (start == lo) start++; for ( ; start &lt; hi; start++) &#123; T pivot = a[start]; // Set left (and right) to the index where a[start] (pivot) belongs int left = lo; int right = start; assert left &lt;= right; while (left &lt; right) &#123; int mid = (left + right) &gt;&gt;&gt; 1; if (c.compare(pivot, a[mid]) &lt; 0) right = mid; else left = mid + 1; &#125; assert left == right; int n = start - left; // The number of elements to move switch (n) &#123; case 2: a[left + 2] = a[left + 1]; case 1: a[left + 1] = a[left]; break; default: System.arraycopy(a, left, a, left + 1, n); &#125; a[left] = pivot; &#125; &#125; 首先我们看一下传入的参数 // 传入数组，lo开始坐标，hi结束坐标，start是上一步，0加上已经排序好的坐标的后一位 也就是说现在数组有hi个数据，但是从lo到start的数据是已经满足递增的数据了，前面已经看过了，现在我们需要将后面的数据和前面的数据汇合，形成整个递增。 1.如果start==lo，说明前面没有递增数据，所以将坐标加1，为后面准备 2.循环，start以后都是尚未排序的，我们需要将后面的数据插入到前面的有序序列， 3.保存当前位置的数值， 4.得到已经递增的数据的左坐标，也就是lo，得到递增数据的右坐标也即是start 5.然后将当前位置的值与递增数据的中间位置的数值进行比较，如果小于，那么说明当前值是插在递增数据的前半段，将递增数据的右坐标改成中间位置的坐标，如果是大于，那么是后半段，将递增数据的左坐标改成中间位置，直到找到正确位置，判断当前值的位置与正确位置的差，如果小与2则进行移动，如果大于则进行拷贝 6.循环将以上数值全部弄完，这样我们就可以得到整个有序数列了。 我们现在回到第三步的第一小步，如果比较器为null， 进入sort(a, fromIndex, toIndex); 3.112345678//对没有比较器的数组进行比较public static void sort(Object[] a, int fromIndex, int toIndex) &#123; rangeCheck(a.length, fromIndex, toIndex); if (LegacyMergeSort.userRequested) legacyMergeSort(a, fromIndex, toIndex); else ComparableTimSort.sort(a, fromIndex, toIndex, null, 0, 0); &#125; 3.21234567891011121314//进入了ComparableTimSort的sort方法 ，传入数组，起始坐标与结束坐标。static void sort(Object[] a, int lo, int hi, Object[] work, int workBase, int workLen) &#123; assert a != null &amp;&amp; lo &gt;= 0 &amp;&amp; lo &lt;= hi &amp;&amp; hi &lt;= a.length; int nRemaining = hi - lo; if (nRemaining &lt; 2) return; // Arrays of size 0 and 1 are always sorted // If array is small, do a "mini-TimSort" with no merges if (nRemaining &lt; MIN_MERGE) &#123; int initRunLen = countRunAndMakeAscending(a, lo, hi); binarySort(a, lo, hi, lo + initRunLen); return; &#125; 这里面的几步如 int initRunLen = countRunAndMakeAscending(a, lo, hi); binarySort(a, lo, hi, lo + initRunLen); 都与上面的几步差不多，不过内部的比较方法不一样 12345678910111213141516171819private static int countRunAndMakeAscending(Object[] a, int lo, int hi) &#123; assert lo &lt; hi; int runHi = lo + 1; if (runHi == hi) return 1; // Find end of run, and reverse range if descending if (((Comparable) a[runHi++]).compareTo(a[lo]) &lt; 0) &#123; // Descending while (runHi &lt; hi &amp;&amp; ((Comparable) a[runHi]).compareTo(a[runHi - 1]) &lt; 0) runHi++; reverseRange(a, lo, runHi); &#125; else &#123; // Ascending while (runHi &lt; hi &amp;&amp; ((Comparable) a[runHi]).compareTo(a[runHi - 1]) &gt;= 0) runHi++; &#125; return runHi - lo; &#125; 因为比较器为null，所以会将值转成比较器进行比较，存储的值，都是实现了Comparable接口的类，内部的比较方法也都重写了，会按照自带的比较。 在最后的比较阶段也是一样的。 123456789101112131415161718192021222324252627282930313233private static void binarySort(Object[] a, int lo, int hi, int start) &#123; assert lo &lt;= start &amp;&amp; start &lt;= hi; if (start == lo) start++; for ( ; start &lt; hi; start++) &#123; Comparable pivot = (Comparable) a[start]; // Set left (and right) to the index where a[start] (pivot) belongs int left = lo; int right = start; assert left &lt;= right; while (left &lt; right) &#123; int mid = (left + right) &gt;&gt;&gt; 1; if (pivot.compareTo(a[mid]) &lt; 0) right = mid; else left = mid + 1; &#125; assert left == right; int n = start - left; // The number of elements to move // Switch is just an optimization for arraycopy in default case switch (n) &#123; case 2: a[left + 2] = a[left + 1]; case 1: a[left + 1] = a[left]; break; default: System.arraycopy(a, left, a, left + 1, n); &#125; a[left] = pivot; &#125; &#125; Arrays.sortArrays.sort在上面已经被调用过了，可以看一下 这样我们就把比较器的代码大致看完了 ，看这个源码，我是传数据debug看的，这样数据改变更方面看一些，这里面我比较喜欢，是在数据排序的时候，这个是直接插入法，但是它优化了一点是，插入不是一个一个比较，而是和有序序列的中间值比较，这样更快。看完之后我对比较器的理解更深了一层。]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[常用排序算法(java版)]]></title>
    <url>%2F2018%2F11%2F09%2F%E5%B8%B8%E7%94%A8%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95-java%E7%89%88%2F</url>
    <content type="text"><![CDATA[常见排序算法简介交换类排序： 快速排序 冒泡排序 插入类排序 直接插入排序 希尔排序 选择类排序： 选择排序 堆排序 归并排序： 二路归并排序 总结： 归并排序最快，其次是快速排序和堆排序，两者不相伯仲，但是有一点需要注意，数据初始排序状态对堆排序不会产生太大的影响，而快速排序却恰恰相反。 附一张算法复杂度图片 代码实现声明： 本片博客的的排序结果全部为递增。 交换类快速排序思路： 从待排序的数组中选择一个数据为基准数，通常选择数组第一位 把小于基准数的放在基准数左边，把大于基准数的放在右边，中间放基准数，然后基准数左右两边数组分别进行循环此步骤。 具体步骤： 快速排序，在数组中选定一个基准数，一般选择数组第一位，分两个方向查询，一个从左至右（索引为0），一个从右至左（索引为数组长度），先从右向左查询比基准数小的数（直到右边的索引和左边的索引相遇），查询到之后将此数赋值给基准数（记住此数，后面要用），然后开始从左至右查询比基准数大的数（知道左边的索引与右边的索引相遇），查询到之后将这个数赋值给刚刚的记住的数的位置，直到两索引相遇，然后将基准数赋值给中间值。这就完成了一次快排，然后从这个位置将数组分为左右两部分，分别进行快速排序。 代码实现： 123456789101112131415161718192021222324252627282930313233343536373839404142/** * 快速排序，将传入的数组进行排序，从右至左查找比基准数小的，从左至右查找比基准数大的，然后交换位置 * @param arrs * @param start 数组的开始位置 * @param end 数组的结束位置 */public void qkSort(int arrs[],int start,int end)&#123; if (end &lt;= start)&#123; return; &#125; //默认第一个值为基准数 int tem = arrs[start]; //记录下开始的坐标 int left = start; int right = end; //开始比较，两个位置不可以相等 while (end != start)&#123; //如果右边的比基数大，坐标就一直递减，直到小于，或者达到和左边的位置一样 while (arrs[end]&gt;=tem &amp;&amp; start&lt;end)&#123; --end; &#125; //如果找到了右边比基数大的，将大的值赋给基数的位置， arrs[start] = arrs[end]; //然后从左至右扫描，如果左边比基数小，坐标就增加，直到找到比基数大的，或者坐标相等 while (arrs[start]&lt;=tem &amp;&amp; start&lt;end)&#123; ++start; &#125; //将左边这个大的，放在右边的那个大的位置 arrs[end] = arrs[start]; &#125; //将基准数放在中间位置 arrs[start] = tem; //打印 log(arrs); qkSort(arrs,left,start-1); qkSort(arrs,start+1,right);&#125; 冒泡排序思路： 从数组第一个数据向后，两两比较，如果前面一个数据大，那么交换位置，直到最后，将最大值挪到最后一个位置完成一次循环 后面循环还是从第一个数据向后比较，两两比较，但是不需要比较到数组最后一位，比较到数组长度减去循环次数 具体步骤： 从第一个数据开始向后比较，如果前一个大就交换位置，如果后一个大就不交换,这是第一次，将最大值转移到了最后一位，然后开始第二轮，从第一个数据开始比较,但是至于要比较到倒数第二个数据，因为最后一个已经是最大的了 代码实现： 12345678910111213141516171819202122/** * 冒泡排序 * @param arrs 数组 */ public void bubblesort(int arrs[])&#123; log(arrs); //循环的次数 for (int i=0;i&lt;arrs.length-1;i++)&#123; //当前数据的坐标，只需要比较到数组长度减去循环次数 for (int j=0;j&lt;arrs.length-1-i;j++)&#123; //交换位置 if (arrs[j]&gt;arrs[j+1])&#123; int tem = arrs[j+1]; arrs[j+1] = arrs[j]; arrs[j] = tem; &#125; &#125; &#125; //打印 log(arrs); &#125; 插入类直接插入排序思路： 将未排序数组插入有序数组，默认无序数组的第一位为有序数组。 具体步骤： 将无序数组的第一位认为是有序数组，然后从第二位开始将数据插入有序数组，如果第二位大于前面一位，则前两位数据是有序的，索引增加，将第三位数据与前面一个数据比较，如果第三位小于前面一位，则将前面一位数据赋值给第三位，然后索引减少，直到找到合适的位置，将数据插入 代码实现： 12345678910111213141516171819202122/** * 直接插入排序 * @param arrs 数组 * @param len 数组长度 */ public void insertSort(int arrs[],int len)&#123; for (int i=1;i&lt;len;i++)&#123; //保存当前位置的数据 int tem = arrs[i]; int j=i-1; //如果后面一个数小于前面一个数，就交换位置 while (j&gt;=0 &amp;&amp; arrs[j]&gt;tem)&#123; arrs[j+1] = arrs[j]; j--; &#125; arrs[j+1]=tem; log(arrs); &#125; &#125; 希尔排序希尔排序是对直接插入排序的增强版，直接插入需要循环n-1次，每次都需要与前面的数据循环比较， 而希尔排序采用了增量的方式对直接插入排序改进 思路： 首先设置增量为数组的一半，然后根据增量进行分组 在每个组内进行直接插入排序 然后增量减少，循环以上两步，直至增量小于1 代码实现： 123456789101112131415161718192021222324public void shellSort(int arrs[])&#123; if (arrs.length==0)&#123; return; &#125; //第一次设置增量为数组长度的一半 int incrementNum = arrs.length/2; while (incrementNum &gt;= 1)&#123; for (int i=0;i&lt;arrs.length;i++)&#123; //对数据进行插入排序 for (int j=i;j&lt;arrs.length;j+=incrementNum)&#123; int tem = arrs[j]; int m = j; while (m-incrementNum&gt;=0 &amp;&amp; arrs[m]&lt;arrs[m-incrementNum])&#123; arrs[m] = arrs[m-incrementNum]; m-=incrementNum; &#125; if (m!=j)&#123; arrs[m] = tem; &#125; &#125; &#125; incrementNum /=2; &#125; 选择类选择排序思路： 假设最大值得索引为数组第一位，然后循环比较数组后面的值与最大索引的值，如果后面的值比较大记录下最大值索引，然后继续比较后面的值与最大值索引的值，循环到最后，则最大值索引处的值就是数组的最大值，将该数据与数组最后一位交换，第一次比较完成 假设最大值索引为第一位，然后重复上一步，不过只需要循环比较到倒数第二位，因为倒数第一位已经是最大值了，然后将这次的最大值和倒数第二位交换， 循环以上步骤即可 代码实现： 123456789101112131415161718public void selectSort(int arrs[])&#123; int maxIndex ; for (int i =0;i&lt;arrs.length;i++)&#123; //每次循环，默认最大值索引为第一位 maxIndex = 0; for (int j=1;j&lt;arrs.length-i;j++)&#123; //如果当前位置大于最大值，将最大值的坐标改成这次的。 if (arrs[j]&gt;arrs[maxIndex])&#123; maxIndex = j; &#125; &#125; //将这次最大值与数组后面进行交换 int tem = arrs[maxIndex]; arrs[maxIndex] = arrs[arrs.length-1-i]; arrs[arrs.length-1-i] = tem; &#125; &#125; 堆排序思路 首先需要将数组建成一个堆，（大根堆） 将该数组的第一个数据与数组最后一个数据交换 将数组的长度减一的数组再次建堆，再次交换数据， 循环，直至最后 代码实现 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768/** * 开始堆排序 * @param arrs */ public void heapSort(int arrs[])&#123; for (int i=0;i&lt;arrs.length;i++)&#123; maxHeap(arrs,arrs.length-i); int tem = arrs[0]; arrs[0] = arrs[arrs.length-1-i]; arrs[arrs.length-1-i] = tem; &#125; log(arrs); &#125; /** * 构建大顶堆 * @param arrs */ public void maxHeap(int arrs[],int size)&#123; for (int i=arrs.length-1;i&gt;=0;i--)&#123; initHeap(arrs,i,size); &#125; &#125; /** * 构建堆，如果父节点比子节点小，需要重新构建 * @param arrs 数组 * @param currentNodeIndex 当前节点索引 * @param size 数组长度 */ public void initHeap(int arrs[],int currentNodeIndex,int size)&#123; if (currentNodeIndex&gt;=size)&#123; return; &#125; int leftIndex = 2*currentNodeIndex+1; int rightIndex = 2*currentNodeIndex+2; //假设最大的节点就是现在这个节点 int max = currentNodeIndex; //判断坐标是否出界，也即是他有没有左子节点 if (leftIndex&lt;size)&#123; //如果左边的节点大，记录坐标 if (arrs[max]&lt;arrs[leftIndex])&#123; max = leftIndex; &#125; &#125; if (rightIndex&lt;size)&#123; if (arrs[max]&lt;arrs[rightIndex])&#123; max = rightIndex; &#125; &#125; //说明当前堆不是正确的，需要重新构建 if (max != currentNodeIndex)&#123; int tem = arrs[currentNodeIndex]; arrs[currentNodeIndex] = arrs[max]; arrs[max]=tem; initHeap(arrs,max,size); &#125; &#125; 归并排序思路： 归并排序是先将数组分，然后治，将一个数组分成单个的数组，然后对相邻数组进行归并 代码实现： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859/** * 归并排序 * @param arrs 数组 * @param left 数组的开始索引 * @param right 数组的结束索引，开始是数组长度-1 */ public void mergeSort(int arrs[],int left,int right)&#123; if (left&lt;right)&#123; int min = (right+left)/2; mergeSort(arrs,left,min); mergeSort(arrs,min+1,right); merge(arrs,left,min,right); log(arrs); &#125; &#125; /** * 归并数组 * @param arrs 数组 * @param left 第一个数组的开始坐标 * @param min 第一个数组的结束坐标，+1也是第二个数组开始坐标 * @param right 第二个数组的结束坐标 */ public void merge(int arrs[],int left,int min,int right)&#123; //声明一个临时数组，和这两个数组加起来一样大的数组 int ar[] = new int[right-left+1]; //第一个数组的开始坐标 int i=left; // 第二个数组的开始索引 int j=min+1; // 参考坐标 int k=0; while (i&lt;=min &amp;&amp; j&lt;=right)&#123; //如果第一个数组的值比第二数组的值大，那么僵第二个数组的值加入 临时数组 if (arrs[i]&gt;=arrs[j])&#123; ar[k++] = arrs[j++]; &#125;else &#123; ar[k++] = arrs[i++]; &#125; &#125; //如果第一个数组还有值，将数据全部加入临时数组 while (i&lt;=min)&#123; ar[k++] = arrs[i++]; &#125; while (j&lt;=right)&#123; ar[k++] = arrs[j++]; &#125; // 将临时数组中的数据，重新赋值给真正的数组 for (int k2 = 0; k2 &lt; ar.length; k2++) &#123; arrs[k2 + left] = ar[k2]; &#125; &#125;]]></content>
      <categories>
        <category>java</category>
        <category>算法</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>算法，排序</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[笔试真题]]></title>
    <url>%2F2018%2F09%2F29%2F%E7%AC%94%E8%AF%95%E7%9C%9F%E9%A2%98%2F</url>
    <content type="text"></content>
  </entry>
  <entry>
    <title><![CDATA[第二篇测试]]></title>
    <url>%2F2018%2F09%2F16%2F%E7%AC%AC%E4%BA%8C%E7%AF%87%E6%B5%8B%E8%AF%95%2F</url>
    <content type="text"></content>
  </entry>
  <entry>
    <title><![CDATA[我的第一条博客]]></title>
    <url>%2F2018%2F09%2F16%2F%E6%88%91%E7%9A%84%E7%AC%AC%E4%B8%80%E6%9D%A1%E5%8D%9A%E5%AE%A2%2F</url>
    <content type="text"><![CDATA[1.新建页面在根目录下使用以下命令即可生成博客页面，在_post文件夹下 1hexo new "博客标题" 2.编辑博客该文件是markdown格式的 在文件开始会先编辑此博客的基本信息，标题，时间，标签，分类，简介之类的，下面就是写正文 1234567891011121314151617181920212223242526272829title:/* 文章标题，可以为中文 */date:/* 建立日期，如果自己手动添加，请按固定格式** 自定义文章发布的时间*/categories:/* 分类，支持多级，比如：- technology- computer- computer-aided-art*/tags:/* 标签** 多个可以这样写[标签1,标签2,标签3]*/description:/* 文章的描述，在每篇文章标题下方显示** 建议每篇文章都务必加上！*/keywords:/* 关键字，并且作为网页的 keywords 元数据*/ 3. 发布使用以下命令 12hexo s -g //本地使用hexo d -g //发布到远程的GitHub]]></content>
      <categories>
        <category>Hexo教程</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>博客</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello World]]></title>
    <url>%2F2018%2F09%2F16%2Fhello-world%2F</url>
    <content type="text"><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub. Quick StartCreate a new post1$ hexo new "My New Post" More info: Writing Run server1$ hexo server More info: Server Generate static files1$ hexo generate More info: Generating Deploy to remote sites1$ hexo deploy More info: Deployment]]></content>
  </entry>
</search>
